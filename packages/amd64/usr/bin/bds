#!/bin/bash
PIDS="$(echo $$)"
echo "$PIDS" > /tmp/bds-common.pid.txt
echo "$PIDS do BDS-common"
echo 'Minecraft Bedrock Server Starting'
cd /opt/BDS-Common/
export LD_LIBRARY_PATH=/opt/BDS-Common
./bedrock_server | grep -v 'Running AutoCompaction...'
